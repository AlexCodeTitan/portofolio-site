import styled from "styled-components";

export const AppWrapper = styled.div`
  min-height: 100vh;
  max-width: 100vw;
  padding-top: 60px;
  overflow: hidden;
  background-color: var(--white);
  color: var(--black);
`;
